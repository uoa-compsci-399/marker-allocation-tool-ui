export interface CourseData {
  courseCoord: string;
  courseID: string;
  courseName: string;
  semester: string;
  closingDate: string;
  availableSpots: string;
  daysLeft: string;
  maxSpots: string;
}
